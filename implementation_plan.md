# 연한 녹색 테마 및 세로형 2단 레이아웃 개편 구현 계획

전체 서비스의 색상 테마를 연한 녹색(주 색상)과 어두운 딥 그린/차콜(보조 색상) 컬러 스킴으로 변경하고, 본문 영역의 레이아웃을 기존 가로 배열에서 수직 세로 배열로 변경합니다.

## User Review Required

> [!IMPORTANT]
> **테마 및 컬러 스킴 상세**:
> - **주 색상 (Primary)**: 맑고 부드러운 파스텔톤 연한 녹색 (`#69db7c` 또는 `#8ce99a`)
> - **보조 색상 (Secondary)**: 주 색상과 대비가 뛰어난 딥 포레스트 그린 차콜 (`#1e2d24`)
> - **헤더 스타일**: 주 색상(연한 녹색 그라데이션)을 배경으로 적용하고, 가독성을 위해 어두운 보조 색상 텍스트를 적용합니다.
> - **버튼 규칙**: 기본 버튼("시작하기")의 배경색은 주 색상(연한 녹색)으로 설정하고, 마우스 호버 시 보조 색상(어두운 딥 그린)으로 반전 전환되도록 설정합니다.
> - **레이아웃**: 데스크톱과 모바일 공통으로 본문 영역의 섹션 1(기능 소개)과 섹션 2(후기 목록)가 수직으로 쌓여 흐르도록 `.viewport-wrapper`를 세로 컬럼 정렬로 변경합니다.

## Open Questions

*없음. 요구사항에 맞추어 레이아웃과 색상 스킴을 정확히 정비하겠습니다.*

## Proposed Changes

### [HTML & JavaScript] 변경 없음 (로직 동일 유지)

### [CSS] 테마 변수 및 레이아웃 수정

#### [MODIFY] [styles.css](file:///Users/iloveisyou/Desktop/Project/AI/antigravity/day1/styles.css)
- `:root` 테마 변수 수정:
  - 주 색상 `--color-accent-primary` 및 보조 색상 컬러 스킴 변경.
  - 헤더 섀도우 및 파티클용 포인트 컬러 변경.
- `body` 및 `.viewport-wrapper` 레이아웃 수정:
  - `.viewport-wrapper`를 `flex-direction: column;`으로 변경하여 두 섹션이 수직으로 쌓이도록 유도.
  - 가로 폭과 간격(`gap: 3.5rem`) 최적화.
- `.app-header` (헤더) 스타일 변경:
  - 파란색 배경에서 연한 녹색 그라데이션(`linear-gradient(90deg, #8ce99a, #69db7c)`)으로 변경.
  - 텍스트 색상을 어두운 보조 색상(`#1e2d24`)으로 변경하여 명도 대비 확보.
- 기본 버튼(`.interactive-btn` 단독) 스타일 변경:
  - 기본 배경색: 연한 녹색 (`var(--color-accent-primary)`)
  - 기본 글자색: 어두운 보조 색상 (`#1e2d24`)
  - 호버 배경색: 어두운 보조 색상 (`#1e2d24`)
  - 호버 글자색: 화이트 (`#ffffff`)
- 동적 테마 버튼(`.interactive-btn.next-btn`)의 기본 구조와 호버 트랜지션이 기본 버튼 개편과 상충되지 않도록 규칙 재정립.

---

## Verification Plan

### Automated Tests
- 없음

### Manual Verification
- 수정 후 로컬 브라우저에서 화면 상태 확인.
- 헤더의 파란 배경이 연한 녹색으로 변경되고 글자가 또렷이 보이는지 확인.
- 본문의 두 섹션이 위아래로 순서대로 정렬되는지 확인.
- "시작하기" 버튼의 평소 색상이 연한 녹색이며 호버 시 어두운 숲속 색상으로 잘 바뀌는지 확인.
- "다음 문장 보기" 버튼은 동적 색상 변경 기능이 깨짐 없이 잘 작동하는지 확인.
